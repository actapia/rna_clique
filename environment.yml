name: rna-clique
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.12
  - pip
  - tqdm
  - pandas
  - joblib>=1.3.2
  - networkx
  - matplotlib
  - seaborn
  - scikit-bio
  - pytables
  - biopython
  - more-itertools
  - dendropy
  - pyyaml
  - adjusttext
  - numpy
  - psutil
  - sympy
  - pysam
  - pip:
      - nice-colorsys
      - simple-blast>=0.7.0
      - python-sat[aiger,approxmc,cryptosat,pblib]
      # For simulator:
      - multiset-key-dict
  # For simulator:
  - scipy
