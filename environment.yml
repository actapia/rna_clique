name: rna-clique
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - tqdm
  - pandas
  - joblib>=1.3.2
  - networkx
  - matplotlib
  - seaborn
  - scikit-bio
  - pytables
  - pip:
      - biopython
      - dendropy
      - nice-colorsys
      - simple-blast==0.0.5
      # scikit-bio 0.6.0 (in development)
#      - git+https://github.com/scikit-bio/scikit-bio.git@df931a0f518f105cb648b29fe38b2a114b5ece96
      # For simulator:
      - more_itertools
      - PyYAML
  # For simulator:
  - scipy
